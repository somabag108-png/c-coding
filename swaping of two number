#include <stdio.h>

int main() {
    int a = 5, b = 10, temp;

    temp = a;  // store value of a
    a = b;     // assign b to a
    b = temp;  // assign temp to b

    printf("After swapping:\n");
    printf("a = %d, b = %d", a, b);

    return 0;
}
